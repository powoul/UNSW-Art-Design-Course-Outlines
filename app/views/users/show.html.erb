<%= content_for(:breadcrumbs) do -%>
	<li><span class="divider">/</span><%= link_to params[:role] == "students" ? "All Students" : "All Staff", users_path %></li>
	<li><span class="divider">/</span><%= @user.fullname %></li>
<% end -%>

<div class="page-header">
	<h1><%= @user.fullname %> </h1>
</div>

<%= render :partial => @user %>