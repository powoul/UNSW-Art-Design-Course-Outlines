<div class="form-inputs">
	<div class="control-group <%= 'error' if f.object.errors[:role].any? %>">
		<label class="control-label required">User Role</label>
		<div class="ceontols">
			<%= f.input :role, :label => false, :collection => User::ROLES %>
		</div>
	</div>

</div>

  
